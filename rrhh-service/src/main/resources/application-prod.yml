spring:
  datasource:
    url: ${RRHH_DB_URL:jdbc:postgresql://localhost:5432/rrhh_db}
    username: ${RRHH_DB_USERNAME:postgres}
    password: ${RRHH_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.sql: INFO
    pe.albrugroup.rrhh_service: INFO
