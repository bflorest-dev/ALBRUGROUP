export interface UpdatePreventaPayload {
  nombreTitular: string;
  celularRegistro: string;
  celularReferencia: string;
  correo: string;
}

export interface TipificarLeadPayload {
  codigoTipificacion: string;
  codigoSubtipificacion: string;
}
