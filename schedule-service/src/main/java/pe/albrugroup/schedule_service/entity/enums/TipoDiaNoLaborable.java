package pe.albrugroup.schedule_service.entity.enums;

/** Motivo de un dia no laborable / override de calendario. */
public enum TipoDiaNoLaborable {
    FERIADO,
    VACACIONES,
    PERMISO,
    DESCANSO_EQUIPO
}
