package pe.albrugroup.schedule_service.entity.enums;

/** Sub-estados repetibles cronometrados que se registran como sesiones (total derivado por suma). */
public enum TipoSesionEstado {
    SERVICIOS,
    PAUSA_ACTIVA,
    CAPACITACION
}
